# 55FRIES 작업 규칙

## 중고나라 키워드 알림

- 신규 알림과 가격 수정은 `productStartPrice`를 기본 `100000`원으로 설정한다.
- 사용자가 명시적으로 바꾸라고 하지 않는 한 0원, `null`, 10만 원 미만의 하한은 쓰지 않는다.
- 알림 최고가는 최근 90일 판매완료 평균가의 85% 이하로 설정하고 1만 원 단위로 내림한다.
- 최근 90일 판매완료 표본이 없으면 평균가를 추측하지 않고 기존 알림 상한을 유지한다.
- 평균가는 중고나라 `판매가(EXECUTION)` 시계열에서 모델별 최소 정상가 미만과 중앙값의 0.5~1.75배 밖의 이상값을 제외한 뒤 계산한다.
- 사용자가 직접 만들어둔 기존 알림은 요청 범위에 포함되지 않으면 수정하거나 삭제하지 않는다.
- 로그인 세션과 토큰은 Git, JSON, 로그에 남기지 않고 macOS 키체인 `com.55fries.joongna.session`에서만 읽는다.
- 계정 설정을 바꾼 뒤에는 목록을 다시 조회해 키워드 수와 가격 범위를 검증한다.

## 가격 가이드

- `3개월 평균`은 최근 90일 판매완료 가격의 산술평균이며 표본 수를 함께 저장한다.
- 모델 전체 키워드로 수집한 평균은 용량·크기·구성 혼합값임을 사이트 비고에 밝힌다.
- 판매완료 표본이 0건이면 `표본 미확보`로 두고 임의 값을 만들지 않는다.
